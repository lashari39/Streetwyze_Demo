<div class="container">
	<% flash.each do |key, value| %>
		<%= content_tag :div, class: "alert alert-#{key == 'notice' ? 'success' : 'danger'}" do %>
			<%= value %>
   	<% end %>
  <% end %>
</div>
